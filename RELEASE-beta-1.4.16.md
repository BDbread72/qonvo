# beta-1.4.16 — 서버 이미지 업로드 자가치유 + 버전 표기 수정

서버 보드에 이미지를 넣었을 때 "업로드가 안 된 것처럼" 보이던 문제를 잡았습니다.

## 🐛 수정

- **서버 보드에 넣은 이미지가 빈 카드로 보이던 문제** — 조사 결과 파일 업로드 자체는
  서버에 정상 저장되고 있었고, 넣은 쪽 클라이언트가 이미지를 **화면에 못 그려서**
  실패처럼 보였습니다(즉시 디코드 실패 시 미리보기·크기 없이 빈 카드).
  - 디코드를 내용 기반 감지로 한 번 더 시도하고, 실패 원인을 로그에 남깁니다.
  - 그래도 실패하면 백그라운드 로더가 성공하는 즉시 카드 크기를 잡고
    서버에 정정 데이터(미리보기 포함)를 다시 보내 **자동 복구**합니다.
  - 서버 업로드가 실제로 실패한 경우도 이제 로그에 경고로 남아 진단할 수 있습니다.
- **exe 버전이 `beta-X.Y.Z+ECHO is on.` 으로 표시되던 문제** — 빌드 스크립트의
  빌드번호 굽기 버그. 이제 `beta-1.4.16+<빌드번호>` 로 올바르게 표시됩니다.

## ✅ 점검

- 서버 E2E 25/25, 커서스킨 16/16, 이미지 자가치유 스모크 16/16 통과.
- UPnP 자동 포트개방 자가복구(임대 갱신 실패 → 재발견) 정상 동작 확인.

클라이언트 위주 수정입니다(협업 서버 프로토콜 변경 없음).

# beta-1.4.12 — Dynamic Cursor (커서 스킨)

내 커서를 직접 디자인해서 협업 중인 다른 사람에게 **내 커서로 보이게** 하는 기능을 추가했습니다.
마인크래프트 스킨처럼 계정(merri)에 저장돼 어디서 접속하든 따라옵니다.

## ✨ Dynamic Cursor — 커서 스킨

- **내 커서 디자인** — 설정 → 협업 → "내 커서 스킨 → 디자인…"
  - 기본 제공 **프리셋 5종**(클래식·그래파이트·오션·선셋·민트) 클릭 한 번으로 적용
  - 간단 에디터(모양·색) 또는 **PNG 직접 업로드**
- **역할 세트** — 진짜 커서처럼 상황에 따라 자동으로 바뀝니다
  - 기본(화살표) · 노드 위에선 **손모양** · 입력 중엔 **I-beam**
- **서로 보임** — 같은 기능을 쓰는 상대에게 내가 낀 커서가 그대로 보입니다(내 커서도 캔버스 위에서 스킨으로 보임)
- 계정(merri 프로필)에 저장 — 다른 기기에서 접속해도 따라옴. 구버전/미설정은 기본 화살표로 안전하게 표시

## 🧩 그 외

- chat 노드를 '메신저'에서 **'LLM CALL 노드'**로 재설계 — 결과는 출력 포트로만 내보내고,
  작동 중 시각효과(글로우/셰이머/타이핑점/실행 테두리, 깜빡임 없음) 추가, 생성옵션 항상 표시
- 서버 프로토콜 v2(하위호환 — 구버전 클라/서버도 그대로 접속)

## 📝 참고

- 프리셋 아이콘 출처: Phosphor Icons(MIT), Lucide(ISC)
- 커서 스킨은 **서버 모드(협업)**에서 동작합니다

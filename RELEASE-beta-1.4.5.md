## 서버 모드 대규모 안정화 + 크래시 보고 시스템

이번 버전은 협업(서버) 모드의 실사용 버그를 대거 잡고, 앱이 어떤 식으로 죽든
원인을 파악할 수 있는 크래시·오류 보고 체계를 새로 넣었습니다.

### 새 기능
- **크래시·오류 자동 보고**: 앱이 크래시하거나 처리되지 않은 오류가 나면, 그 내용을
  접속한 서버로 보내 운영자가 모아볼 수 있습니다(서버 미접속 시 다음 접속 때 전송).
  설정 → 고급에서 끌 수 있습니다(기본 켜짐).
- **모든 비정상 종료 감지**: 강제 종료·세그폴트·전원 차단처럼 예외 훅을 거치지 않는
  종료까지 다음 실행에서 사후 감지해 보고합니다(앱이 "조용히 사라진" 경우도 추적).
- **채팅 노드 인라인 응답**: 이제 로그창을 열지 않아도 노드 면에서 응답이 바로 보이며,
  생성 중에는 글자가 실시간으로 차오릅니다. 실패는 빨간 "오류" 상태로 구분됩니다.

### 협업(서버) 모드 버그 수정
- **한 번에 접속이 안 되고 재시도해야 되던 문제 수정**(서버가 첫 접속자에게 지연을
  떠넘기던 원인 제거).
- **여러 명이 동시에 접속할 때 오류 나며 안 들어가지던 문제 완화**(접속 순간 서버가
  잠깐 멈추던 원인 제거 + 접속 중 들어온 변경의 유실/꼬임 방지).
- **서버에서 생성한 이미지가 노드에 안 붙던 문제 수정.**
- **AI 진행 상황(생성 중)이 화면에 안 뜨던 문제 수정.**
- **버튼·신호가 다른 참가자에게 전달되지 않던 문제 수정**(버튼을 누르면 다른 사람
  화면의 전구·게이트도 함께 동작).
- **다른 사용자 커서 잔상이 남던 문제 수정**(보드를 옮겨도 옛 커서가 사라집니다).
- 협업 패널(아바타 로딩)에서 드물게 앱이 죽던 문제 방어.
- AI 이미지 생성 결과가 모든 참가자에게 중복 카드로 만들어지던 문제 수정.

### 서버 운영
- 콘솔에 `reports [N]` 명령 추가 — 클라가 보낸 최근 크래시/오류를 열람합니다.
- 동시 접속·대형 보드 join 시 이벤트 루프가 얼지 않도록 스냅샷/노드수 캐싱.

## 다운로드
- **Windows**: `qonvo.exe` — 받아서 바로 실행
- **Server**: `qonvo-server-dist.zip` — 압축 풀고 `qonvo-server.sh`(Linux/Mac) 또는
  `qonvo-server.bat`(Windows) 실행. Python만 있으면 첫 실행 시 자동 설치됩니다.

# beta-1.4.15 — 커서 스킨 깜빡임 완전 제거

[beta-1.4.13](https://github.com/BDbread72/qonvo/releases/tag/beta-1.4.13)의 커서 스킨 후속 패치입니다.

## 🐛 수정

- **마우스 포인터(커서 스킨)가 계속 깜빡이던 문제** — 원인이 두 가지였고 모두 제거했습니다.
  1. 1.4.13의 자가복구가 0.12초마다 커서를 통째로 새로 만들어 갈아끼우던 것 →
     이제 한 번 만든 커서를 재사용하고, 이미 스킨이면 아무것도 하지 않습니다.
  2. 노드/버튼 위에서 마우스를 움직일 때마다 Qt가 손모양 커서로 되돌리고 0.12초 뒤에야
     스킨이 복귀하던 것(손↔스킨 교대 깜빡임) → 커서가 바뀌는 **즉시** 같은 이벤트 안에서
     스킨을 복원해, 눈에 보이는 교체 자체가 없습니다.
- 방사형 메뉴가 열려 커서를 숨긴 동안은 그대로 존중하고, 닫히는 순간 스킨으로 즉시 복귀합니다.

## 📦 참고

- 클라이언트 전용 수정입니다(협업 서버 변경 없음).
- 이 릴리스에는 미공개였던 **beta-1.4.14 — 채팅 명령어 시스템 대확장**(`/create` 데이터태그,
  `/function` 매크로+CodeMirror 에디터, `/execute` 조건부, `/data` 저장소, 견고성 하드닝)이
  함께 포함됩니다. 상세는 저장소의 `RELEASE-beta-1.4.14.md` 참고.

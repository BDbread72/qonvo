## 버그 수정

- **서버 모드에서 이미지 생성 결과가 다음 노드로 전달되지 않던 문제 수정**: 서버 보드에 접속한 상태에서 Nanobanana 등으로 이미지를 생성하면 노드 면에는 이미지가 떴지만, 자동 이미지 카드 생성과 연결된 노드로의 전파가 동작하지 않았습니다. (솔로 모드는 정상이었습니다.)

## 다운로드

- **Windows**: `qonvo.exe` — 받아서 바로 실행
- **Server**: `qonvo-server-dist.zip` — 압축 풀고 `qonvo-server.sh`(Linux/Mac) 또는 `qonvo-server.bat`(Windows) 실행. Python만 있으면 첫 실행 시 자동 설치됩니다.

## 버그 수정 (서버/협업 모드 집중)

- **숫자·수식·파일 노드가 협업에서 제대로 동기화되지 않던 문제 수정**: 다른 사람이 추가한 Number/Math/File 노드가 상대 화면에 엉뚱한 노드(챗 노드)로 뜨거나, 이동·삭제가 반영되지 않고, 재접속 시 손상되던 문제를 바로잡았습니다.
- **서브보드(Dimension) 노드를 협업 중 새로 추가하면 상대에게 안 보이던 문제 수정.**
- **이미지 생성 결과가 안 풀리던 문제 수정**: 다른 사람이 만든 이미지가 저해상도 미리보기(블러)로만 남고 원본이 안 채워지던 문제를 해결했습니다(첨부 다운로드 후 재로딩 복구).
- **AI 권한·쿼터 제한에 걸린 노드가 "생성 중…"에서 영구히 멈추던 문제 수정**: 이제 오류 메시지를 표시하고 노드를 풀어줍니다.
- **잘못된 채팅 데이터 수신 시 앱이 죽을 수 있던 문제 방어.**
- 협업 안정성 보강: 서버가 적용하지 못한 변경은 다른 참가자에게 전파하지 않음(문서 어긋남 방지), 나간 사용자의 커서 즉시 제거, 첨부 다운로드 임시파일 정리, 서버 메모리 누수 방지.

## 다운로드

- **Windows**: `qonvo.exe` — 받아서 바로 실행
- **Server**: `qonvo-server-dist.zip` — 압축 풀고 `qonvo-server.sh`(Linux/Mac) 또는 `qonvo-server.bat`(Windows) 실행. Python만 있으면 첫 실행 시 자동 설치됩니다.

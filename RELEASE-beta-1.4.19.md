# beta-1.4.19 — 서버 협업 대규모 안정화 + 보안 하드닝

서버 모드 전반을 감사해 **데이터 유실·앱 크래시·보안·누수** 버그를 한 번에 잡았습니다.
클라이언트와 서버 양쪽을 고쳤으므로, **운영 서버(qonvo-server)도 이 버전으로 업데이트**하세요.

## 🔴 데이터 유실 / 크래시 (서버 모드)

- **화면 스크롤만 해도 남의 노드가 사라지던 문제** — 노드가 30개 넘는 보드에서 누군가
  화면을 이동(pan)하면, 그때 그려지는 노드마다 "노드 추가"가 서버로 잘못 재전송돼
  **다른 참가자의 노드가 빈 노드로 덮어써지고 사라졌습니다.** 재실체화 중에는 추가/이동
  op 를 보내지 않도록 막고, 받는 쪽에도 이미 있는 노드는 다시 만들지 않도록 이중으로
  방어했습니다. (이번 사태의 핵심 원인)
- **여러 노드 타입이 서버에 저장되지 않던 문제** — 스티키/이미지카드/디멘션/로직게이트/
  숫자·수식/파일 노드 등 18종은 서버가 노드 id 를 잘못된 키로 저장해, 다시 접속하면
  통째로 사라질 수 있었습니다. id 를 양쪽 키로 저장해 모든 노드 타입이 안전하게
  왕복하도록 고쳤습니다.
- **이미지 카드·파일·디멘션·텍스트·그룹프레임을 끌어 옮겨도 위치가 동기화 안 되던 문제**
  — 이 노드들은 드래그해도 이동이 서버에 전달되지 않아, 재접속하면 원래 자리로
  돌아갔습니다. 이제 위치가 실시간 동기화됩니다.
- **AI 응답 처리 중 앱이 통째로 꺼지던 문제** — AI 진행/완료 콜백에서 예외가 나면 앱이
  즉시 종료될 수 있었습니다(이미지 응답·다운스트림 전파 등). 전면 방어했습니다.
- **손상된 보드로 접속하면 앱이 죽던 문제** — 문제가 있는 스냅샷을 받아도 앱을 죽이지
  않고 부분 복원 후 계속 진행합니다.
- **이미지 카드 더블클릭으로 원본이 안 열리던 문제**(1.4.18 회귀) — 서버/복원 보드의
  상대 경로를 해석해 다시 열립니다.
- **동시에 옮기는 중 크기·위치가 되돌아가던 문제** — 속성 동기화가 낡은 좌표로 남의
  이동을 덮던 것을 막았습니다(위치는 이동 op 가 단독 권위).

## 🔒 보안 (서버)

- **다른 보드의 이미지에 접근되던 문제** — 첨부 다운로드/업로드 토큰이 보드 범위로
  묶여 있지 않아, 참가하지 않은 보드의 이미지를 읽거나 덮어쓸 수 있었습니다. 이제 지금
  참가 중인 보드로만 접근이 허용됩니다.
- **인증 토큰 누수** — 같은 연결에서 재인증하면 이전 토큰이 폐기되지 않고 무한히 쌓여,
  끊긴 뒤에도 접근이 유효했습니다. 재인증을 거부합니다.
- **게스트 권한 상승** — 게스트 허용(allow_guests) 서버에서, 관리자 페이지/설정으로
  승격된 계정 이름을 **비밀번호 없이** 대면 그 권한(운영자까지)을 얻을 수 있었습니다.
  게스트는 이제 기본 레벨을 넘길 수 없습니다.

## 🧹 누수 / 견고성 (서버)

- 기본 설정(레이트 무제한)에서 AI 사용 기록이 무한히 쌓이던 메모리 누수 수정
- 보드 전환 시 이전 보드에 유령 참가자가 남던 문제 수정(떠남 통지)
- UPnP 자동 포트개방 갱신 루프가 일시적 오류 한 번에 영영 멈추던 문제 수정(자가복구 유지)
- 무효 op(이미 삭제된 노드 대상 등)가 기록·전파돼 oplog 를 부풀리던 문제 제거
- 서버 시딩 스냅샷을 원자적으로 저장(중단 시 손상 방지)
- 운영자 kick/레벨변경이 유저의 여러 접속 중 하나에만 적용되던 문제 수정
- 이미지 모델 판정 실패 시 이미지 게이트가 열리던 것을 닫힘으로(fail-closed)
- 설정의 문자열 레벨(`"2"`)이 조용히 무시되던 문제 수정
- 서버 크래시 로깅에 unraisable 훅 추가(누락돼 있던 GC 예외까지 기록)
- 로컬 노드 삭제 시 동기화 캐시 항목이 안 지워지던 누수 수정

## 참고

- 클라이언트끼리의 재방송 억제 효과는 접속 중인 참가자가 **모두 이 버전 이상**일 때
  완전합니다.
- 공유 챗 노드의 대화 히스토리 동기화(사용자 발화 전파/포맷 통일)는 두 클라이언트로
  실제 검증이 필요해 다음 릴리스에서 별도로 다룹니다.

## 플러그인 스토어 — 사이트 카탈로그 + 인앱 원클릭 설치

AI 프로바이더 플러그인(Claude·GPT 등)을 공식 사이트에서 둘러보고, 앱 안에서 바로
설치·업데이트할 수 있게 됐습니다. 이제 플러그인은 앱에 박혀 배포되지 않아도 공유할 수
있습니다.

### 플러그인 둘러보기 (앱)
- **설정 → 플러그인 → "플러그인 둘러보기 / 업데이트"** — 온라인 목록에서 원클릭 설치.
- **버전 관리** — 설치된 플러그인보다 새 버전이 올라오면 "업데이트 가능"으로 표시되고,
  버튼 한 번으로 갱신됩니다.
- **안전한 다운로드** — 받은 파일을 sha256 으로 검증한 뒤에만 설치합니다.
- 설치 후에는 기존처럼 목록에서 체크해 활성화하고 API 키를 넣으면 됩니다.

### 플러그인 페이지 (사이트)
- 공식 사이트에 **플러그인 카탈로그**가 생겼습니다(이름·버전·지원 모델·다운로드).
- 앱이 없거나 수동으로 넣고 싶을 때 `.py` 파일을 직접 받아 plugins 폴더에 두면 됩니다.

> 플러그인은 임의 코드를 실행합니다. 신뢰할 수 있는 출처의 플러그인만 설치하세요.

## 다운로드
- **Windows**: `qonvo.exe` — 받아서 바로 실행
- **Server**: `qonvo-server-dist.zip` — 압축 풀고 `qonvo-server.sh`(Linux/Mac) 또는
  `qonvo-server.bat`(Windows) 실행. Python만 있으면 첫 실행 시 자동 설치됩니다.

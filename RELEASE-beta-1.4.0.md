## 새로운 기능

- **노드 이름 통일**: 모든 노드에 공통 편집형 이름표가 생겼습니다. 더블클릭으로 이름을 바꾸고, 저장·서버 동기화에 그대로 반영됩니다. 면을 어지럽히던 `#id` 라벨은 제거했습니다.
- **채팅 명령어 시스템**: 캔버스에서 Enter 채팅·커서 말풍선에 `/명령어`를 입력하면 자동완성과 고스트 텍스트가 뜹니다. 솔로·서버 모드 모두 지원합니다.
- **보드 스크린샷**: `Ctrl+Shift+P`로 현재 캔버스를 PNG로 저장하거나 클립보드에 복사합니다. 협업 시 다른 사람 커서 이름을 `Guest N`으로 익명화하는 옵션을 함께 제공합니다.
- **서버 AI 거버넌스**: 권한 레벨별로 사용 가능한 모델·이미지 생성·요청 속도·동시 실행·쿼터를 제한할 수 있습니다. 사용량은 콘솔과 웹 관리자에서 회계로 확인합니다. 기본값은 무제한이라 기존 서버와 완전히 호환됩니다.

## 변경 사항

- Upscale·Remove-BG 노드를 코어에서 분리했습니다(추후 플러그인으로 재추가 예정).
- 서버 배포 zip에 `build.toml`을 포함해 `/health`의 서버 버전 표기가 정상화됩니다.

## 다운로드

- **Windows**: `qonvo.exe` — 받아서 바로 실행
- **Server**: `qonvo-server-dist.zip` — 압축 풀고 `qonvo-server.sh`(Linux/Mac) 또는 `qonvo-server.bat`(Windows) 실행. Python만 있으면 첫 실행 시 자동 설치됩니다.

## 서버 아이콘 + 버그 수정 모음

### 새 기능
- **서버 아이콘(마인크래프트식)**: 서버가 자신의 아이콘을 광고하고, 서버 목록·보드 선택창에 썸네일로 표시됩니다. 기본은 흰 라운드 타일 + 로고라 어떤 배경에서도 잘 보입니다. 운영자는 서버 데이터 폴더에 `server-icon.png`(1:1 권장)를 넣어 교체할 수 있습니다.

### 버그 수정 (데이터 유실·크래시 집중)
- **숫자/수식/파일 노드에 연결된 선(엣지)이 저장/불러오기 시 사라지던 문제 수정.**
- **숫자/수식 노드를 삭제해도 다시 저장하면 되살아나던 문제 수정.**
- **다른 사람이 보낸 잘못된 변경 1건이 앱 전체를 종료시킬 수 있던 문제 방어**(협업 중 안정성).
- **이미지 생성 응답이 비정상일 때 앱이 죽을 수 있던 문제 방어.**
- **Claude(Anthropic) 모델 + 라운드테이블/시스템 프롬프트 조합에서 오류 나던 문제 수정.**
- 파일 노드 삭제 시 임시 첨부가 남던 문제, 이름 지정 후 삭제한 노드의 이름 정보가 남던 문제 정리.
- 서버 접속 후 보드 선택창이 잠깐 멈추던 문제 개선(불필요한 재조회 제거).

### 서버 운영
- **AI 동시 실행 한도가 N후보(preferred) 생성을 제대로 세도록 수정**(한도 우회 방지).
- 배치 실행에서 `-m 모델` 지정이 실제 실행 모델에 정확히 반영되도록 수정(메타 표기 일치).
- 관리자 Mattermost 검색 토큰에 만료 시간 부여(메모리 잔류 방지).
- 협업 채팅 명령어 자동완성의 잠재적 예외 처리 보강.

## 다운로드
- **Windows**: `qonvo.exe` — 받아서 바로 실행
- **Server**: `qonvo-server-dist.zip` — 압축 풀고 `qonvo-server.sh`(Linux/Mac) 또는 `qonvo-server.bat`(Windows) 실행. Python만 있으면 첫 실행 시 자동 설치됩니다.

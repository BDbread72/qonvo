## 채팅 대개편 + 협업 안정화 + 성능

이번 버전은 채팅(보드 챗 노드 / People 메신저) 경험을 크게 손보고, 협업 모드의
버튼·신호 동기화를 근본적으로 다시 설계했으며, 대화 로딩 속도를 끌어올렸습니다.

### 채팅 노드 (보드 위 AI 채팅)
- **전형적인 채팅 UI로 재설계** — 말풍선 대화창 + 하단 인라인 입력칸(Enter 전송).
  매번 화면 중앙에 입력 팝업이 뜨던 흐름을 없앴습니다.
- **실시간 응답** — 생성 중 글자가 말풍선 안에서 바로 차오릅니다(로그창 안 열어도 됨).
  실패는 빨간 말풍선으로 구분.
- **깜빡임 제거** — 생성 중 노드 전체가 깜빡이던 현상을 없앴습니다.

### People (merri 메신저)
- **대화 로드 빠르게** — 한 번 본 대화는 다시 열 때 네트워크를 기다리지 않고 즉시
  표시하고, 최신만 백그라운드로 동기화합니다. DM 채널 조회 왕복도 제거.
- **작은 창 깜빡임 수정** — 대화를 전환할 때 아주 작은 빈 창이 잠깐 떴다 사라지던
  버그를 고쳤습니다.

### 협업(서버) 모드
- **버튼·신호 동기화 재설계** — 한쪽에서 버튼을 누르거나 스위치를 켜면 다른 참가자
  화면의 전구·게이트도 정확히 따라옵니다. 신호가 유실·재정렬돼도 최종 상태가
  어긋나지 않도록 '레벨 기반' 모델로 바꿨습니다.
- **대화/보드 로드 압축** — 접속 시 받는 보드 데이터를 압축해 텍스트(대화) 위주
  보드의 로딩이 크게 빨라집니다(서버도 이 버전이어야 적용).

### 안정성
- 협업 패널의 비동기 이미지 로딩에서 드물게 앱이 죽던 문제 방어.
- 크래시·오류가 나면(강제 종료·세그폴트 등 비정상 종료 포함) 접속한 서버로 보고를
  보내 운영자가 원인을 파악할 수 있습니다(설정에서 끌 수 있음).

## 다운로드
- **Windows**: `qonvo.exe` — 받아서 바로 실행
- **Server**: `qonvo-server-dist.zip` — 압축 풀고 `qonvo-server.sh`(Linux/Mac) 또는
  `qonvo-server.bat`(Windows) 실행. Python만 있으면 첫 실행 시 자동 설치됩니다.

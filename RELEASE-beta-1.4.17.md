# beta-1.4.17 — 커서가 가리키는 지점 교정 + LLM 노드 크기 존중

"커서가 가리키는 위치가 이상하다 / 노드 크기를 바꾸려 해도 잘 안 된다"를 잡았습니다.
두 문제는 겹쳐 있었습니다 — 클릭 지점이 어긋난 커서로 작은 리사이즈 핸들을 조준하니
자꾸 빗나가고, 노드는 크기를 바꿔도 자동 맞춤이 되돌렸습니다.

## 🐛 수정

- **커서 스킨의 클릭 지점(핫스팟) 교정** — 지금까지 모든 커서가 이미지의 좌상단 모서리를
  클릭 지점으로 썼습니다. 노드 위에서 뜨는 손가락 커서는 보이는 손끝과 실제 클릭점이
  **약 11px 어긋나** 있었고(리사이즈 핸들·포트 같은 작은 UI가 잘 안 잡히던 원인),
  텍스트(I-beam) 커서는 더 컸습니다. 이제 글리프 내용에서 가리키는 지점을 자동 감지합니다
  (화살표=팁, 손=손끝, I-beam=중앙). 업로드한 커스텀 스킨에도 동일하게 적용되고,
  다른 사람 화면에 보이는 내 커서도 같은 기준으로 정확히 그려집니다.
- **LLM 노드가 크기를 되돌리던 문제** — 리사이즈 핸들로 크기를 바꿔도 모델 변경 등
  자동 맞춤이 노드 높이를 원래대로 스냅시켰습니다. 이제 **직접 정한 크기(저장된 크기 포함)는
  절대 덮어쓰지 않고**, 내용이 짜부되지 않는 최소 높이만 보장합니다.

클라이언트 전용 수정입니다(협업 서버 변경 없음).

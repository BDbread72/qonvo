# beta-1.4.20 — 서버에서 실행한 대화 로그가 재접속하면 사라지던 문제 해결

"챗 노드를 한 번 실행해서 '완료 · 1회 실행 · 로그 보기'가 떴는데, 껐다 켜면(재접속)
결과가 사라지고 '대기 중'으로 돌아간다"의 뿌리를 잡았습니다.

## 🐛 원인

서버가 AI 응답을 보드 문서에 저장할 때 **두 가지 문제**가 겹쳐 있었습니다.

1. **재접속용 스냅샷 캐시가 갱신되지 않았습니다.** 서버는 접속자에게 보낼 전체 보드
   스냅샷을 성능을 위해 캐시하는데, AI 응답 저장은 이 캐시를 무효화하지 않았습니다.
   그래서 재접속(항상 전체 동기화) 시 **AI 실행 이전에 만들어진 옛 스냅샷**이 그대로
   전달돼, 방금 실행한 대화가 통째로 빠졌습니다 → 로그 없음 → "대기 중".
2. **저장 포맷이 클라이언트와 달랐습니다.** 서버는 응답을 내부용 포맷으로 저장했는데
   클라이언트 로그 화면은 다른 포맷을 읽어, 설령 전달되더라도 빈 줄로 보였습니다.

## 🔧 수정

- AI 응답 저장 시 재접속 스냅샷 캐시를 무효화 → 재접속하면 실행한 대화가 그대로
  복원됩니다("완료 · N회 실행 · 로그 보기" 유지).
- 응답을 클라이언트와 동일한 포맷(사용자 발화 + 응답 + 모델 + 토큰)으로 저장 →
  로그 보기에 내용이 정상 표시됩니다.

## 참고

- 서버·클라 모두 업데이트 대상입니다(운영 서버도 이 버전으로).
- 함께 접속한 다른 참가자의 **실시간** 대화 표시는 재접속 시 정상 복원되지만, 실행
  순간의 라이브 반영은 다음 릴리스에서 더 다듬습니다.

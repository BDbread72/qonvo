# beta-1.4.21 — 재접속 후 이미지 후보를 선택해도 아무것도 안 생기던 문제 해결

서버에서 여러 후보(preferred)로 이미지를 만든 뒤 **껐다 켜서(재접속)** 후보를 골랐는데
결과 이미지가 만들어지지 않던 문제를 잡았습니다.

## 🐛 원인

재접속하면 후보 이미지가 상대경로(`attachments/…`)로 복원됩니다. 그런데 선택 시 결과
이미지를 만드는 코드가 이 경로를 **절대경로도 base64도 아니라고 판단**해, base64로
디코드하려다 실패하고 **아무것도 만들지 않고 조용히 끝났습니다.** (라이브로 방금 만든
경우엔 base64/임시파일이라 정상 동작 → 재접속 후에만 발생.)

## 🔧 수정

- 후보 선택 → 이미지 카드 자동 생성(`_auto_create_next_node`), 연결된 이미지 카드 갱신
  (`_update_image_card`), 디멘션 추가(`_add_to_dimension`) 세 경로 모두 상대참조를 보드
  임시폴더에서 해석하도록 수정. 아직 다운로드 전이면 참조를 그대로 카드에 넘겨 카드가
  나중에 스스로 해석합니다(카드는 항상 생성됨).

## 참고

- 클라이언트 전용 수정입니다(서버 무관 — 운영 서버 재배포 불필요).
- beta-1.4.20의 서버 수정(대화·후보 로그 영속)과 함께 써야 완전합니다.

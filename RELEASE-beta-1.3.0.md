실시간 협업 서버가 들어온 가장 큰 업데이트입니다. 이제 같은 보드를 여러 명이 동시에 보고 편집하고, AI 실행을 서버가 대신 돌립니다. 숫자 데이터 노드도 새로 추가됐고, 저장/접속 안정성을 대폭 끌어올렸습니다.

## 🌐 실시간 협업 (서버 모드)
- 같은 보드를 여러 명이 동시에 보고 편집 — 노드/엣지/텍스트가 실시간 동기화
- 서버 → 보드 브라우저: 한 서버 안의 여러 보드(회의록·기획 등)를 골라 입장
- 라이브 커서 + 이름표, 영역 선택 표시, 접속자 목록(F1), 디버그 오버레이(F12)
- 보드 채팅 + People DM
- AI를 서버가 대신 실행 — 내 PC에 키가 없어도 서버 키로 GPT·Claude·Gemini 사용
- 이미지 첨부 동기화 — 드래그/붙여넣은 이미지가 서버에 저장되고 모두에게 보임
- 포트포워딩 없이 외부 접속(UPnP 자동 개방 + 끊겨도 자가복구)
- 릴레이 접속(스팀 방식) — 잠긴 망에서도 직접 우선·릴레이 폴백
- 웹 관리자 페이지(/admin, Operator 전용): 사용자 권한·화이트리스트·밴 관리, merri(Mattermost) 로그인 연동

## 🔢 새 노드: 숫자 데이터
- Number 노드: 값 저장 + 증가/리셋 신호로 카운터처럼 사용
- Math 노드: 사칙연산·비교(>, <, = 등) → 결과/참 신호 출력
- 전용 NUMBER 포트 + 값이 바뀌면 연결된 노드로 자동 전파

## 🛡️ 안정성 — 저장 / 불러오기
- 1MB가 넘는 이미지가 서버에 저장되지 않던 문제 수정 (조용한 업로드 실패 → 영구 유실)
- 큰 보드에 누군가 들어올 때 다른 사람 접속이 끊기던(ping/pong) 문제 수정
- 손상된 보드 스냅샷이 빈 보드로 덮어써지지 않도록 보존 + 자동 복구
- 전원 손실에도 안전하도록 디스크 기록 강화(fsync)
- 노드 삭제 시 남던 끊긴 연결선 정리

## 🔁 버전 호환 체크
- 클라이언트 ↔ 서버 버전 핸드셰이크 추가
- 서버가 요구 버전을 정하면, 너무 오래된 클라이언트는 명확한 안내와 함께 접속 차단
- 서버가 더 최신이면 "업데이트 권장" 안내 표시

## 🐞 버그 수정
- 여러 입력칸에 커서가 동시에 깜빡이고, 빈 공간을 눌러도 안 사라지던 포커스 문제 수정
- 채팅 노드가 드물게 무한 생성되던 문제 원천 차단
- 협업 중 텍스트 입력 시 커서가 앞으로 튀던 문제 수정
- 라이브 커서가 줌/DPI에 따라 어긋나던 문제 근본 수정
- AI 오류를 노드에 표시 + 폐기된 모델 정리
- .qonvo ↔ 폴더 내보내기/가져오기 추가

---

설치: 아래 `qonvo.exe`(Windows)를 받아 실행하세요. 서버를 직접 돌리려면 `qonvo-server-dist.zip`(Python만 있으면 자동 설치)을 받으세요.

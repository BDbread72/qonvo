# beta-1.4.18 — 서버 보드에서 노드 크기가 사라지던 문제 해결

"크기를 바꿔 놓아도 다시 접속하면 원래대로 돌아온다"의 뿌리를 뽑았습니다.
서버 협업 동기화의 구조 결함 3가지가 겹쳐 있었고, 전부 클라이언트 수정으로
해결됩니다(서버 코드·프로토콜 변경 없음).

## 🐛 수정

- **이미지 카드 / 디멘션 리사이즈가 서버에 저장 안 되던 문제** — 두 노드 타입은
  속성 동기화에서 통째로 제외돼 있어, 크기를 아무리 바꿔도 서버 보드에 기록되지
  않았습니다. 이제 크기·가리기·제목이 실시간 동기화되고 재접속 후에도 유지됩니다.
  (첨부 이미지 자체는 기존 업로드 경로가 그대로 담당 — 로컬 경로가 서버 기록을
  오염시키지 않게 크기 등 경량 필드만 보냅니다.)
- **접속 직후 모든 노드를 서버로 "재방송"해 남의 편집을 되돌리던 문제** — 보드에
  들어오면 첫 주기 스캔이 방금 받은 노드 전부를 서버로 다시 보냈습니다. 클라이언트마다
  미세하게 다른 렌더 값·낡은 값이 서버 원본을 덮어써 **다른 멤버가 조절한 크기가
  슬그머니 되돌아가는** 핵심 원인이었습니다. 이제 받은 상태를 기준선으로 기록하고
  **내가 실제로 바꾼 것만** 전송합니다. (접속 시 불필요한 op 폭주가 사라져 큰 보드의
  oplog 비대·재생성 깜빡임도 함께 줄어듭니다.)
- **화면 밖 노드로 온 다른 멤버의 편집이 유실되던 문제** — 큰 보드에서 아직 화면에
  그려지지 않은(지연 로딩 대기 중) 노드에 도착한 리사이즈/편집/삭제/엣지 변경이 조용히
  버려졌습니다. 나중에 그 자리로 스크롤하면 접속 시점의 낡은 모습으로 살아났고, 위
  재방송 문제와 결합해 낡은 값을 서버로 되밀기까지 했습니다. 이제 대기 중인 노드에도
  전부 반영됩니다(위치·크기·내용·삭제·채팅 누적·엣지).

## 🔧 기타

- 원격 op 적용 실패 시 방어 로그가 오히려 앱을 죽일 수 있던 잠복 버그(logger 미임포트) 수정
- 크기 조절 op마다 첨부 목록을 다시 긁던 낭비 제거(첨부 참조가 왔을 때만 동기화)

클라이언트 전용 수정입니다. 재방송 차단 효과는 접속 중인 클라이언트가 모두
이 버전 이상일 때 완전해집니다.
